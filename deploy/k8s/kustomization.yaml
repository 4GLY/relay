apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: relay

resources:
  - namespace.yaml
  - relay-secrets-sealed.yaml
  - relay-registry-creds-sealed.yaml
  - deployment.yaml
  - service.yaml
