apiVersion: v1
kind: Service
metadata:
  name: relay-api
  namespace: relay
  labels:
    app: relay-api
spec:
  type: ClusterIP
  selector:
    app: relay-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
