# PR Diff Summary

- Clarify that Relay's HTTP API is the product contract.
- Keep MCP and CLI as adapters over the same contract.
- Avoid wrapper-only behaviors that cannot be verified through `/v1/*`.
