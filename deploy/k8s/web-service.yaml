apiVersion: v1
kind: Service
metadata:
  name: relay-web
  namespace: relay
  labels:
    app: relay-web
spec:
  type: ClusterIP
  selector:
    app: relay-web
  ports:
  - name: http
    port: 80
    targetPort: 3000
