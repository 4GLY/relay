# Relay Go API base URL (bearer-auth at /v1/* and /mcp).
NEXT_PUBLIC_RELAY_API_URL=http://localhost:8080

# Public base URL the web surface is served from. Used to build sharable URLs.
NEXT_PUBLIC_RELAY_PUBLIC_BASE_URL=http://localhost:3000
